package constants

// 全局常量
const (
	SharedDir      = "./shared"             // 项目根目录下的共享目录
	TFTPDir        = "tftp-root"            // TFTP 根目录，相对于 SharedDir
	ConfigFile     = "./configs/config.ini" // 配置文件路径
	DefaultTimeout = 30                     // 默认超时时间，单位秒
)
