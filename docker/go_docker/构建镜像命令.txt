构建
docker build -t go-dev-env .

构建完成后，可以使用以下命令运行容器
docker run -it --rm -v $(pwd):/app -w /app go-dev-env


    -it：以交互模式运行容器。

    --rm：容器退出后自动删除。

    -v $(pwd):/app：将当前目录挂载到容器的 /app 目录。

    -w /app：设置工作目录为 /app。

    go-dev-env：使用的镜像名称。
